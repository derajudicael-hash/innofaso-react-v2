# Optionnel — uniquement nécessaire si le frontend et le backend sont
# déployés sur des domaines/ports différents en production.
# En dev (et si front/back partagent le même domaine en prod derrière un
# reverse-proxy), laisser vide : le proxy Vite (dev) ou le chemin relatif
# "/api" (prod) suffit.
# VITE_API_URL=https://api.mondomaine.com/api
